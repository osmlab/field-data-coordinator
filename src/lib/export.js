'use strict'

module.exports.xml = (observationIds, filename) => {
  console.log(observationIds, filename)
}

module.exports.json = (observationIds, filename) => {
  console.log(observationIds, filename)
}

module.exports.geojson = (observationIds, filename) => {
  console.log(observationIds, filename)
}
